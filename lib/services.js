export const modalService = {
     onReset: () => {
        setShowAppOptions(false);
      },
    
       onAddSticker: () => {
        // we will implement this later
      },
    
       onSaveImageAsync: async () => {
        // we will implement this later
      },
}